# Dashboard UI/HTML/Enrichment Archive

This directory contains historical dashboard UI, HTML, and enrichment documents retired by `RETIRE_DASHBOARD_UI_HTML_AND_ENRICHMENT`.

Phase 2 removed the active dashboard/enrichment dev_tools, builders, exporters, UI/HTML paths, diagnostics, and direct tests. These documents are retained only as historical context.

Current `dc_*` source facts, legacy Datacenter Markdown/CSV reports, current `ec_*`, `ec_source_layer`, migrations `002`/`003`, and the read-only dashboard DB cleanup preflight remain outside this archive.
